<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Post</title>
    <link href="/css/index.css" rel="stylesheet" />
  </head>

  <body class="bg-slate-100 text-slate-800 min-h-screen">

    <!-- Navbar -->
    <nav class="bg-blue-600 text-white px-6 py-4 shadow">
      <h1 class="text-2xl font-bold">Postify</h1>
    </nav>

    <!-- Main Container -->
    <main class="max-w-2xl mx-auto mt-10 bg-white p-8 rounded-lg shadow">

      <h2 class="text-2xl font-semibold text-slate-900 mb-6">
        Edit Your Post
      </h2>

      <!-- Post Info -->
      <div class="mb-6 text-sm text-slate-600 space-y-1">
        <p>
          <span class="font-medium">Post ID:</span> <%= post.id %>
        </p>
        <p>
          <span class="font-medium">Username:</span> @<%= post.username %>
        </p>
      </div>

      <!-- Edit Form -->
      <form
        action="/post/<%= post.id %>?_method=PATCH"
        method="post"
        class="space-y-4"
      >
        <textarea
          name="content"
          rows="6"
          class="w-full p-4 border border-slate-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
        ><%= post.content %></textarea>

        <div class="flex gap-4">
          <button
            type="submit"
            class="px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition"
          >
            Save Changes
          </button>

          <a
            href="/post/<%= post.id %>"
            class="px-6 py-2 bg-slate-200 text-slate-800 rounded-lg hover:bg-slate-300 transition"
          >
            Cancel
          </a>
        </div>
      </form>

    </main>
  </body>
</html>
